# Generated by velnor-actions-generator. DO NOT EDIT.
name: Package update

on:
  schedule:
    - cron: "17 */6 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  jackin-project:
    if: ${{ github.repository_owner == 'jackin-project' }}
    uses: jackin-project/velnor-actions/.github/workflows/package-updater.yml@ab9c27f393a4b2c581dde7134e2635b2d7ce78bf # 2026.8.10
    with:
      consumer-repository: ${{ github.repository }}
      channel: stable
      current-signer-digest: "1e062d5bbe329873047ee8a8e79bba0811e53b65"
      old-signer-digest: ""
      old-signer-activated-at: ""
      old-signer-expires-at: ""
      branch: "package-update/verified/stable"
    secrets:
      app_private_key: ${{ secrets.PACKAGE_UPDATER_APP_PRIVATE_KEY }}
  tailrocks:
    if: ${{ github.repository_owner == 'tailrocks' }}
    uses: tailrocks/velnor-actions/.github/workflows/package-updater.yml@d1efb0f66c25f21f0d3823d1414b0fac57bd9015 # 2026.8.10
    with:
      consumer-repository: ${{ github.repository }}
      channel: stable
      current-signer-digest: "1e062d5bbe329873047ee8a8e79bba0811e53b65"
      old-signer-digest: ""
      old-signer-activated-at: ""
      old-signer-expires-at: ""
      branch: "package-update/verified/stable"
    secrets:
      app_private_key: ${{ secrets.PACKAGE_UPDATER_APP_PRIVATE_KEY }}
  ChainArgos:
    if: ${{ github.repository_owner == 'ChainArgos' }}
    uses: ChainArgos/velnor-actions/.github/workflows/package-updater.yml@7e7f7432a107157e1af730ec8e4d8da1d85f1f82 # 2026.8.10
    with:
      consumer-repository: ${{ github.repository }}
      channel: stable
      current-signer-digest: "1e062d5bbe329873047ee8a8e79bba0811e53b65"
      old-signer-digest: ""
      old-signer-activated-at: ""
      old-signer-expires-at: ""
      branch: "package-update/verified/stable"
    secrets:
      app_private_key: ${{ secrets.PACKAGE_UPDATER_APP_PRIVATE_KEY }}
  package-update-required:
    if: ${{ always() }}
    needs: [jackin-project, tailrocks, ChainArgos]
    runs-on: ubuntu-latest
    steps:
      - env:
          OWNER: ${{ github.repository_owner }}
          JACKIN: ${{ needs.jackin-project.result }}
          TAILROCKS: ${{ needs.tailrocks.result }}
          CHAINARGOS: ${{ needs.ChainArgos.result }}
        run: |
          set -euo pipefail
          case "$OWNER" in
            jackin-project) test "$JACKIN" = success; test "$TAILROCKS" = skipped; test "$CHAINARGOS" = skipped ;;
            tailrocks) test "$JACKIN" = skipped; test "$TAILROCKS" = success; test "$CHAINARGOS" = skipped ;;
            ChainArgos) test "$JACKIN" = skipped; test "$TAILROCKS" = skipped; test "$CHAINARGOS" = success ;;
            *) echo "unrecognized owner" >&2; exit 1 ;;
          esac
